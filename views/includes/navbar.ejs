<!-- Premium Glass Navbar -->
<nav class="navbar navbar-expand-lg sticky-top navbar-glass">
  <div class="container-fluid px-4 px-lg-5">
    <!-- Brand -->
    <a class="navbar-brand d-flex align-items-center gap-2" href="/listings">
      <i class="fa-solid fa-compass text-accent fs-4"></i>
      <span class="brand-text">WanderHub</span>
    </a>

    <!-- Mobile Toggler & Search Trigger -->
    <div class="d-flex align-items-center gap-3 d-lg-none">
      <button class="btn-icon-glass" id="mobileSearchToggle" aria-label="Open Search">
        <i class="bi bi-search"></i>
      </button>
      <button class="navbar-toggler border-0 p-0 shadow-none" type="button" data-bs-toggle="offcanvas"
        data-bs-target="#premiumMobileMenu" aria-controls="premiumMobileMenu" aria-expanded="false"
        aria-label="Toggle navigation">
        <div class="hamburger-icon">
          <span></span>
          <span></span>
          <span></span>
        </div>
      </button>
    </div>

    <!-- Desktop Menu & Actions -->
    <div class="collapse navbar-collapse" id="desktopMenu">
      <!-- Centered Links -->
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0 gap-1">
        <li class="nav-item">
          <a class="nav-link custom-nav-link active" href="/listings">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link custom-nav-link" href="#all-listing">Destinations</a>
        </li>
        <li class="nav-item">
          <a class="nav-link custom-nav-link" href="/listings/new">Add Listing</a>
        </li>
        <li class="nav-item">
          <a class="nav-link custom-nav-link" href="#about_us">About</a>
        </li>
      </ul>

      <!-- Right Side: Search & Auth -->
      <div class="d-flex align-items-center gap-3">
        <!-- Desktop Search Bar -->
        <div class="search-pill d-none d-xl-flex">
          <i class="bi bi-search text-muted ms-3"></i>
          <input type="text" class="form-control border-0 bg-transparent shadow-none"
            placeholder="Search destinations...">
        </div>

        <!-- Auth Buttons -->
        <% if(!currentUser) { %>
          <% if(!locals.hideAuthButtons) { %>
            <% if(!locals.isSignupPage) { %>
              <a href="/login" class="btn btn-link text-dark text-decoration-none fw-semibold">Log in</a>
            <% } %>
            <% if(!locals.isLoginPage) { %>
              <a href="/signup" class="btn btn-premium-primary rounded-pill px-4">Sign Up</a>
            <% } %>
          <% } %>
        <% } else { %>
          <div class="d-flex align-items-center gap-3">
            <div class="user-avatar bg-premium-primary text-white rounded-circle d-flex align-items-center justify-content-center fw-bold shadow-sm" style="width: 40px; height: 40px; font-size: 1.2rem; background: linear-gradient(135deg, #0d6efd, #0dcaf0);">
              <%= currentUser.username.charAt(0).toUpperCase() %>
            </div>
            <a href="/logout" class="btn btn-outline-dark rounded-pill px-4">Log Out</a>
          </div>
        <% } %>
      </div>
    </div>
  </div>
</nav>

<!-- Premium Mobile Offcanvas Menu -->
<div class="offcanvas offcanvas-end glass-offcanvas" tabindex="-1" id="premiumMobileMenu"
  aria-labelledby="premiumMobileMenuLabel">
  <div class="offcanvas-header border-0 pb-0">
    <h5 class="offcanvas-title brand-text" id="premiumMobileMenuLabel">WanderHub</h5>
    <button type="button" class="btn-close btn-close-dark shadow-none" data-bs-dismiss="offcanvas"
      aria-label="Close"></button>
  </div>
  <div class="offcanvas-body d-flex flex-column pt-4 px-4">

    <!-- Mobile Search -->
    <div class="mobile-search-wrapper mb-4">
      <div class="input-group">
        <span class="input-group-text bg-white border-end-0 rounded-start-pill ps-3"><i class="bi bi-search"></i></span>
        <input type="text" class="form-control border-start-0 rounded-end-pill shadow-none"
          placeholder="Where to next?">
      </div>
    </div>

    <ul class="navbar-nav gap-3 mb-5">
      <li class="nav-item">
        <a class="nav-link mobile-nav-link active" href="/listings">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link mobile-nav-link" href="/listings">Explore</a>
      </li>
      <li class="nav-item">
        <a class="nav-link mobile-nav-link" href="/listings/new">Become a Host</a>
      </li>
      <li class="nav-item">
        <a class="nav-link mobile-nav-link" href="#about">About Us</a>
      </li>
    </ul>

    <div class="mt-auto mb-4 d-grid gap-2">
      <% if(!currentUser) { %>
        <% if(!locals.hideAuthButtons) { %>
          <% if(!locals.isSignupPage) { %>
            <a href="/login" class="btn btn-outline-dark rounded-pill py-2">Log in</a>
          <% } %>
          <% if(!locals.isLoginPage) { %>
            <a href="/signup" class="btn btn-premium-primary rounded-pill py-2">Sign Up</a>
          <% } %>
        <% } %>
      <% } else { %>
        <div class="d-flex align-items-center mb-3">
          <div class="user-avatar text-white rounded-circle d-flex align-items-center justify-content-center fw-bold me-2 shadow-sm" style="width: 40px; height: 40px; font-size: 1.2rem; background: linear-gradient(135deg, #0d6efd, #0dcaf0);">
            <%= currentUser.username.charAt(0).toUpperCase() %>
          </div>
          <span class="fw-semibold"><%= currentUser.username %></span>
        </div>
        <a href="/logout" class="btn btn-outline-dark rounded-pill py-2">Log Out</a>
      <% } %>
    </div>
  </div>
</div>

<!-- Bootstrap JS Bundle -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>